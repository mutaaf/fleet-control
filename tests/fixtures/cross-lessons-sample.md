# CROSS_LESSONS

Auto-generated by `fleet lessons-sync`. Do not edit by hand —
your edits will be overwritten on the next sync. The source of
truth is each project's `docs/LESSONS.md`.

## alpha

### 2026-05-25 — bootstrap

The alpha project is dogfooding itself. The seatbelts: CI gates on
`shellcheck` + `validate`. This first entry is the bootstrap note.

### 2026-05-26 — GitHub Actions sometimes doesn't fire on a freshly-opened PR

A pull_request webhook can silently fail to fire. Push an empty
commit to nudge `synchronize`, or close+reopen the PR.

### undated bootstrap

A header without an ISO date still gets parsed but `date` is null.

## beta

The beta project's notes mostly live as one-liner bullets under the
Entries section below.

### Entries

- 2026-05-21 [ship] vitest pool exhaustion blew up the run -> too many fork workers -> run vitest with `--no-file-parallelism`.
- 2026-05-22 [groom] index-table drift on shipped tickets -> the README index row was missed during the chore -> flip BOTH ticket frontmatter AND index row in the same commit.
- 2026-05-23 [ship] orphan PRs look healable but never merge -> their merge-base with main is empty -> skip orphan PRs in the heal loop; surface them in the summary.
